# Scripts

This directory is reserved for reusable setup, validation, conversion, and
maintenance utilities. It is currently empty.

Keep application code under `src`. A script added here should have safe
defaults, documented inputs and outputs, and no machine-specific paths. Tools
that flash or move hardware must state their prerequisites and recovery steps.
