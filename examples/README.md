# Examples

`simulation/demo/` contains the small trajectory files used by the simulator.

Run the example without opening a window:

```powershell
micromamba run -n aurora-simulation python src/simulation/sim/limb_sim.py --headless
```

A successful run ends with `Playback finished`.
